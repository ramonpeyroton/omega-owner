.vercel

# Environment variables — never commit secrets
.env
.env.local
.env.*.local

# Build output
dist

# Dependency directories
node_modules

# QA reports (generated)
relatorio-qa-*.md

# macOS
.DS_Store
backup-qa-*/
.env*.local

# Claude Code local config (per-machine permissions, worktrees, etc.)
.claude/
