// Thin re-export of the shared Supabase client so the receptionist app
// follows the same pattern as the other role apps (owner/operations/…).
export { supabase } from '../../../shared/lib/supabase';
